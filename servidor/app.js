import express, { Router } from "express"

const app = express()

const calculatorRoute = Router()

calculatorRoute.get("/", )

app.listen(5000, () => {
    log("Servidor corridendo en localhost:", 5000)
})